# Dependencias de Papers_Asistente / GaIA (derivadas de los imports reales).
# Versiones = las instaladas y probadas al 2026-07-09. Instalar con:
#   pip install -r requirements.txt
# OJO: marker-pdf arrastra PyTorch (~pesado); en PC con GPU NVIDIA conviene
# instalar torch con CUDA antes: https://pytorch.org/get-started/locally/

# --- App / API ---
fastapi==0.138.0
uvicorn==0.49.0
pydantic==2.13.4
python-dotenv==1.2.2
requests==2.33.1

# --- RAG / vectores ---
qdrant-client==1.18.0

# --- Ingesta de PDFs ---
marker-pdf==1.10.2
pymupdf==1.28.0
pdfplumber==0.11.9
pillow==10.4.0

# --- Proveedores LLM (visión vía Bedrock) ---
anthropic==0.116.0
boto3==1.43.40

# --- Motor por suscripción (Claude Code CLI) + servidor MCP del corpus (Investigación) ---
# Requiere además el CLI aparte: npm i -g @anthropic-ai/claude-code, y `claude` → /login.
mcp==1.28.1
